# QLoRA training configuration (Phase 6). Defaults are sized for a single
# RTX 4080 (16GB). Estimated peak ~9-11GB at these settings.
model:
  base_model_id: "unsloth/Qwen2.5-7B-Instruct-bnb-4bit"   # pre-quantized Qwen2.5-7B-Instruct (Apache 2.0)
  max_seq_length: 2048
  load_in_4bit: true
  dtype: null                # null lets Unsloth pick (bf16 on Ada)
quant:
  bnb_4bit_quant_type: "nf4"
  bnb_4bit_use_double_quant: true
  bnb_4bit_compute_dtype: "bfloat16"
lora:
  r: 16
  alpha: 32                  # 2x rank
  dropout: 0.0
  bias: "none"
  target_modules: ["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]
  use_gradient_checkpointing: "unsloth"   # string, not true: the memory-optimized path
  use_rslora: false
  random_state: 3407
train:
  per_device_train_batch_size: 2
  gradient_accumulation_steps: 8          # effective batch 16
  num_train_epochs: 2
  learning_rate: 0.0002
  lr_scheduler_type: "cosine"
  warmup_ratio: 0.05
  weight_decay: 0.01
  optim: "paged_adamw_8bit"               # paged: optimizer state spills to host RAM on spikes
  max_grad_norm: 1.0
  seed: 3407
  logging_steps: 10
  save_steps: 200
  output_dir: "outputs/adapter"
data:
  train_path: "data/train/train.jsonl"
  val_path: "data/train/val.jsonl"
  chat_template: "chatml"
  train_on_responses_only: true
  instruction_part: "<|im_start|>user\n"
  response_part: "<|im_start|>assistant\n"
  packing: false
save:
  save_adapter_only: true
  merge_16bit: false          # gated; reserved for future vLLM serving
