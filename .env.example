# Copy to .env and fill in. .env is gitignored.

# Frontier teacher for distillation (Phase 5). Provider finalized at Phase 5.
TEACHER_API_KEY=

# Judge for eval entailment (Phase 4). Must be a different model family than the teacher.
JUDGE_API_KEY=
